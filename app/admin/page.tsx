import AdminView from '@/components/Admin/AdminView';

export const metadata = {
  title: 'Household Admin - MealTracker',
  description: 'Household configuration, parameters, members, and audit trail.'
};

export default function AdminPage() {
  return <AdminView />;
}
